<div class="bg-neutral-100 text-black dark:bg-gray-900 dark:text-white w-full tracking-wider px-6 md:px-12 xl:px-16 pt-6 md:pt-10 pb-2">
  <div class="flex justify-between items-center mx-auto">

    <div class="text-purple-700 dark:text-purple-400 hover:text-purple-500 hover:dark:text-purple-200 transition-all ease-out duration-700">
      <%= link_to '#home' do %>
        <i class="fab fa-wolf-pack-battalion fa-5x"></i>
      <% end %>
    </div>

    <div class="flex items-center space-x-4 md:space-x-8 text-lg">
      <%= render 'shared/button_with_translate', { classes: "nav-header",
                                                   translate: "hover:-translate-y-1" } do %>
        <%= render 'layouts/nav/header', { label: '01.', title: 'About', route: '#about', } %>
      <% end %>
      <%= render 'shared/button_with_translate', { classes: "nav-header",
                                                   translate: "hover:-translate-y-1" } do %>
        <%= render 'layouts/nav/header', { label: '02.', title: 'Experience', route: '#what-i-do' } %>
      <% end %>
      <%= render 'shared/button_with_translate', { classes: "nav-header",
                                                   translate: "hover:-translate-y-1" } do %>
        <%= render 'layouts/nav/header', { label: '03.', title: 'Work', route: '#projects' } %>
      <% end %>
      <%= render 'shared/button_with_translate', { classes: "",
                                                   translate: "hover:-translate-y-1" } do %>
        <%= link_to 'Contact', '#contact', class: "btn-purple px-6 py-3 text-lg"%>
      <% end %>
    </div>

  </div>
</div>