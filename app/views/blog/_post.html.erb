<div>
  <%= link_to blog_post_path(post.slug) do %>
    <h3><%= post.title %></h3>
    <p class="text-sm text-muted"><%= post.date.strftime("%B %d, %Y") %></p>
    <div>
      <%= post.description %>
    </div>
  <% end %>
</div>